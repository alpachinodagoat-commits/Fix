// Standalone links removed per product requirement: the app will not expose a dedicated Standalone Links page.
// If the file is still imported somewhere, we export a no-op component to avoid breaking imports.
export function StandaloneSurveyLinks() {
  return null;
}
